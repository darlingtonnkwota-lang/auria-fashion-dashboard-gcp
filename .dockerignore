# Applies to every Docker build in this repo, regardless of which
# Dockerfile is used -- both app/Dockerfile and frontend/Dockerfile build
# from the repo root as context (see cloudbuild.yaml).
.git/
docs/
pipelines/
*.md
node_modules/
frontend/node_modules/
frontend/.next/
.venv/
__pycache__/
*.pyc
.env
.env.*
!.env.example
